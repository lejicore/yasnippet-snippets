# -*- mode: snippet -*-
# name: type-inline-comment
# key: @ty
# condition: (let ((node (and (fboundp 'treesit-node-at) (ignore-errors (treesit-node-at (point)))))) (not (and node (string= (treesit-node-type node) "comment"))))
# --
/** @type {${type}} */
